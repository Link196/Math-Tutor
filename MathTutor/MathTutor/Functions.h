#ifndef FUNCTIONS_H_
#define FUNCTIONS_H_

#include "User.h"

bool programBody(User*); // the main body of the program
int menu(); // program menu
void changelog(); // program changelog
void addition(User*); // addition subject area
void additionSubtraction(User*); // addition and subtraction in one run
void allSubjects(User*); // all four subjects in one run
void subtraction(User*); // subtraction subject area
void multiplication(User*); // multiplication subject area
void multiplicationDivision(User*); // multiplication and division in one run
void division(User*); // division subject area

#endif
